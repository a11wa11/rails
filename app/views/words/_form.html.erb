<%= form_with(model: word, local: true) do |form| %>
  <% if word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(word.errors.count, "error") %> prohibited this word from being saved:</h2>

      <ul>
      <% word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :english %>
    <%= form.text_field :english, id: :word_english %>
  </div>

  <div class="field">
    <%= form.label :japanese %>
    <%= form.text_field :japanese, id: :word_japanese %>
  </div>

  <div class="field">
    <%= form.label :remarks %>
    <%= form.text_field :remarks, id: :word_remarks %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
